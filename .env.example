# Banco de Dados (Neon / PostgreSQL)
# Obtenha sua string de conexão no console do Neon (ex: postgres://user:pass@host/db?sslmode=require)
DATABASE_URL=

# Gemini API Key (Server-side)
GEMINI_API_KEY=
